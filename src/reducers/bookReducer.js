
export default function bookReducer(state = {books: []}, action ) {

  debugger;
  switch (action.type) {
    case 'FETCH_BOOKS':
      return state
      default:
      return state
  }

  return state
}
